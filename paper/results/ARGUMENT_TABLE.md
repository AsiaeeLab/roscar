# Package argument table (P027)

Generated from current Rd documentation and function formals by `paper/results/arguments.R`. Input types follow documented parameter roles and literal defaults. Validation cells reproduce documented constraints and identify where a parameter's Rd text states none.
The front-door computational default is B=0; the tutorial's teaching and application analyses set B=500. API defaults below are the actual formals. For a `match.arg` choice vector, the first entry is the effective default. Binary link calibration is tested but not simulation-evaluated. Survival and general cluster-randomized inference are unsupported; grouped IDs handle repeated observations/resampling units.

| Argument | Input type | Tutorial default / actual API default | Purpose | Documented validation | Returned output |
|---|---|---|---|---|---|
| `trial_data(X)` | numeric matrix or data frame | `required` | Finite numeric covariate matrix or numeric data frame. | Finite numeric covariate matrix or numeric data frame. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(A)` | numeric treatment vector (0/1 or -1/+1) | `required` | Treatment coded 0/1 or -1/+1. | Treatment coded 0/1 or -1/+1. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(Y)` | numeric vector | `required` | Finite numeric outcome vector. | Finite numeric outcome vector. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(pi)` | numeric probability scalar or vector or NULL | `NULL` | Probability of each participant's observed arm, strictly between zero and one; a scalar is recycled. Defaults to observed arm proportions within strata. This is not always the probability of treatment +1. | Probability of each participant's observed arm, strictly between zero and one; a scalar is recycled. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(id)` | vector of IDs or labels (e.g., character, numeric, factor) or NULL | `NULL` | Independent resampling unit identifier, defaulting to row number. | No additional constraint stated in this parameter's Rd text. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(strata)` | vector of IDs or labels (e.g., character, numeric, factor) or NULL | `NULL` | Randomization/design strata, defaulting to one stratum. | No additional constraint stated in this parameter's Rd text. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(shared)` | character names or integer column indices or NULL | `NULL` | Optional disjoint covariate block names or column indices. Missing block assignments are inferred during transport. | Optional disjoint covariate block names or column indices. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(trial_only)` | character names or integer column indices or NULL | `NULL` | Optional disjoint covariate block names or column indices. Missing block assignments are inferred during transport. | Optional disjoint covariate block names or column indices. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(external_only)` | character names or integer column indices or NULL | `NULL` | Optional disjoint covariate block names or column indices. Missing block assignments are inferred during transport. | Optional disjoint covariate block names or column indices. | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `trial_data(weights)` | numeric vector or NULL | `NULL` | Optional positive finite analysis weights (for example, inverse probabilities of response). These do not replace randomization pi. | Optional positive finite analysis weights (for example, inverse probabilities of response). | A validated list of class `roscar_trial`, including `p_plus`, the probability of assignment to +1, and the covariate `blocks`. |
| `external_data(X)` | numeric matrix or data frame | `required` | Finite numeric covariate matrix or numeric data frame. | Finite numeric covariate matrix or numeric data frame. | A list of class `roscar_external`, compatible with the X/Y/A layout returned by [build_data()]. |
| `external_data(A)` | numeric treatment vector (0/1 or -1/+1) or NULL | `NULL` | Optional treatment vector coded 0/1 or -1/+1. If missing, the arm is designated by `arm_ext` in [fit_external_arms()] or [borrow_cate()]. | Optional treatment vector coded 0/1 or -1/+1. | A list of class `roscar_external`, compatible with the X/Y/A layout returned by [build_data()]. |
| `external_data(Y)` | numeric vector | `required` | Finite numeric outcome vector. | Finite numeric outcome vector. | A list of class `roscar_external`, compatible with the X/Y/A layout returned by [build_data()]. |
| `external_data(shared)` | character names or integer column indices or NULL | `NULL` | Optional disjoint covariate block names or column indices; see [trial_data()]. | Optional disjoint covariate block names or column indices; see [trial_data()]. | A list of class `roscar_external`, compatible with the X/Y/A layout returned by [build_data()]. |
| `external_data(trial_only)` | character names or integer column indices or NULL | `NULL` | Optional disjoint covariate block names or column indices; see [trial_data()]. | Optional disjoint covariate block names or column indices; see [trial_data()]. | A list of class `roscar_external`, compatible with the X/Y/A layout returned by [build_data()]. |
| `external_data(external_only)` | character names or integer column indices or NULL | `NULL` | Optional disjoint covariate block names or column indices; see [trial_data()]. | Optional disjoint covariate block names or column indices; see [trial_data()]. | A list of class `roscar_external`, compatible with the X/Y/A layout returned by [build_data()]. |
| `borrow_cate(trial)` | roscar_trial list | `required` | Trial from [trial_data()], with observed-arm probabilities, IDs, strata, and optional positive analysis weights. | Trial from [trial_data()], with observed-arm probabilities, IDs, strata, and optional positive analysis weights. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(ext)` | roscar_external list | `required` | External source from [external_data()]. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(recipe)` | character choice | `c("A", "B", "C", "D")` | A (matched, two arms), B (one borrowed arm), C (MR-OSCAR imputation), or D (linear CALM with externally supervised PLS). | Choose one of: A, B, C, D. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(learners)` | named list of learners | `list()` | Named list of external, calibration, trial, final, and imputer learners. `trial_plus` and `trial_minus` optionally override individual arms. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(K)` | integer count | `5` | Number of grouped, arm-stratified folds; default 5. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(B)` | integer count | `500` in paper analyses; API `0` | Number of full trial bootstrap resamples; default 0; paper examples 500. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(grid)` | numeric matrix or data frame or NULL | `NULL` | Matrix/data frame of target covariates; defaults to trial rows. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(seed)` | integer scalar or NULL | `NULL` | Integer seed for folds and default learners. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(basis)` | one-sided formula with intercept | `~.` | Prespecified one-sided effect formula, with intercept. | Prespecified one-sided effect formula, with intercept. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(arm_ext)` | numeric arm code (-1 or +1; 0 accepted for control) | `-1` | Borrowed arm for Recipe B, default -1; 0 also means control. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(method)` | character choice or NULL | `NULL` | Transport comparator: `"shared_only"` implements SR-OSCAR; otherwise transport follows the selected recipe. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(shared)` | character column names or NULL | `NULL` | Optional column-name covariate blocks. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(trial_only)` | character column names or NULL | `NULL` | Optional column-name covariate blocks. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(external_only)` | character column names or NULL | `NULL` | Optional column-name covariate blocks. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(embed_dim)` | integer count or NULL | `NULL` | PLS dimension; NULL selects 1--3 by external validation. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(pls_by_arm)` | logical flag | `FALSE` | Fit arm-specific PLS directions instead of the pooled default. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(scale)` | character choice | `c("mean", "link")` | Mean-scale or logistic link-scale calibration. Link calibration needs binary outcome learners and was not evaluated in the paper simulations. | Choose one of: mean, link. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(offset)` | logical flag or NULL | `NULL` | Use the cross-fitted preliminary-contrast correction. Defaults to TRUE for Recipe C and FALSE otherwise. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(folds)` | integer vector of fold labels or NULL | `NULL` | Optional fixed grouped trial fold labels, reused across methods. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(subgroups)` | named list of logical or integer row selectors or NULL | `NULL` | Named logical/index vectors on `grid` for average effects. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `borrow_cate(...)` | additional named arguments | `not supplied` | Additional transport settings. Unknown settings produce an error. | No additional constraint stated in this parameter's Rd text. | A `roscar_fit` with folds, heldout intermediate predictions, final model, paired `trial_only` fit, transport metadata, and optional bootstrap. |
| `learner_lasso(lambda)` | numeric penalty vector | `10^seq(-4, 2, length.out = 25)` | Positive penalty grid, defaulting to 25 log-spaced values from 1e-4 to 1e2. A single value disables tuning. | Positive penalty grid, defaulting to 25 log-spaced values from 1e-4 to 1e2. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_lasso(nfolds)` | integer count | `5` | Number of tuning folds, default five. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_lasso(seed)` | integer scalar or NULL | `NULL` | Optional tuning seed, with no change to the caller's RNG state. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_lasso(family)` | character choice | `c("gaussian", "binomial")` | Either `"gaussian"` or `"binomial"`. | Choose one of: gaussian, binomial. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_lasso(intercept)` | logical flag | `TRUE` | Whether to include an unpenalized intercept. | Whether to include an unpenalized intercept. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_lasso(...)` | additional named arguments | `not supplied` | Additional options to the backend. Core response, offset, fold, and prediction-scale arguments are controlled by the interface. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ridge(lambda)` | numeric penalty vector | `10^seq(-4, 2, length.out = 25)` | Positive penalty grid, defaulting to 25 log-spaced values from 1e-4 to 1e2. A single value disables tuning. | Positive penalty grid, defaulting to 25 log-spaced values from 1e-4 to 1e2. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ridge(nfolds)` | integer count | `5` | Number of tuning folds, default five. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ridge(seed)` | integer scalar or NULL | `NULL` | Optional tuning seed, with no change to the caller's RNG state. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ridge(family)` | character choice | `c("gaussian", "binomial")` | Either `"gaussian"` or `"binomial"`. | Choose one of: gaussian, binomial. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ridge(intercept)` | logical flag | `TRUE` | Whether to include an unpenalized intercept. | Whether to include an unpenalized intercept. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ridge(...)` | additional named arguments | `not supplied` | Additional options to the backend. Core response, offset, fold, and prediction-scale arguments are controlled by the interface. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ols(family)` | character choice | `c("gaussian", "binomial")` | Either `"gaussian"` or `"binomial"`. | Choose one of: gaussian, binomial. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_ols(intercept)` | logical flag | `TRUE` | Whether to include an unpenalized intercept. | Whether to include an unpenalized intercept. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_gam(k)` | integer count | `5` | Basis size for each GAM smooth (automatically limited by the number of distinct observed covariate values). | Basis size for each GAM smooth (automatically limited by the number of distinct observed covariate values). | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_gam(family)` | character choice | `c("gaussian", "binomial")` | Either `"gaussian"` or `"binomial"`. | Choose one of: gaussian, binomial. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_gam(...)` | additional named arguments | `not supplied` | Additional options to the backend. Core response, offset, fold, and prediction-scale arguments are controlled by the interface. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_rf(num.trees)` | integer count | `500` | Number of ranger regression trees. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_rf(seed)` | integer scalar or NULL | `NULL` | Optional tuning seed, with no change to the caller's RNG state. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_rf(...)` | additional named arguments | `not supplied` | Additional options to the backend. Core response, offset, fold, and prediction-scale arguments are controlled by the interface. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_sl(SL.library)` | character vector | `c("SL.mean", "SL.glm")` | SuperLearner library; its constituent packages must be installed by the user. Built-in wrappers resolve in the SuperLearner namespace; custom wrappers can supply their lookup environment through `env` in `...` (including the package's screening function `All`). | SuperLearner library; its constituent packages must be installed by the user. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_sl(nfolds)` | integer count | `5` | Number of tuning folds, default five. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_sl(seed)` | integer scalar or NULL | `NULL` | Optional tuning seed, with no change to the caller's RNG state. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `learner_sl(...)` | additional named arguments | `not supplied` | Additional options to the backend. Core response, offset, fold, and prediction-scale arguments are controlled by the interface. | No additional constraint stated in this parameter's Rd text. | A learner list with `fit`, `predict`, `name`, `family`, and settings. |
| `transport(ext_fit)` | roscar_external_fit object | `required` | A fitted object from [fit_external_arms()]. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(trial)` | roscar_trial list | `required` | A [trial_data()] object. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(method)` | character choice | `c("identity", "impute", "embed", "shared_only")` | One of `identity`, `impute`, `embed`, or `shared_only`. | Choose one of: identity, impute, embed, shared_only. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(shared)` | character names or integer column indices or NULL | `NULL` | Covariate block names or indices; defaults come from the constructors or matching column names. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(trial_only)` | character names or integer column indices or NULL | `NULL` | Covariate block names or indices; defaults come from the constructors or matching column names. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(external_only)` | character names or integer column indices or NULL | `NULL` | Covariate block names or indices; defaults come from the constructors or matching column names. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(imputer)` | learner list with fit/predict functions | `learner_ridge(seed = seed)` | Learner for each external-only covariate, default ridge. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(embed_dim)` | integer count or NULL | `NULL` | Fixed positive number of PLS components, or NULL to select among one to three components. With arm-specific PLS this is per arm. | Fixed positive number of PLS components, or NULL to select among one to three components. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(pls_by_arm)` | logical flag | `FALSE` | Use the optional arm-specific encoder, default FALSE. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(embedding_learner)` | learner list with fit/predict functions | `learner_ridge(seed = seed)` | Learner for external arm regressions in the embedding, default ridge. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(validation_folds)` | integer count | `5` | Number of external validation folds, default five. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `transport(seed)` | integer scalar or NULL | `NULL` | Seed for external validation folds and default learners. | No additional constraint stated in this parameter's Rd text. | A `roscar_transport` list containing `q_plus`, `q_minus`, `q`, `calibration_X`, external validation metadata, and a `predict(newX)` closure returning the same predictions and calibration design for new trial rows. `external_prediction` contains plus/minus predictions on the native external covariates (or embedding) for source-support diagnostics. |
| `fit_cate(trial)` | roscar_trial list | `required` | Validated trial. | No additional constraint stated in this parameter's Rd text. | An object of class `roscar_cate` with fit and fixed basis specification. |
| `fit_cate(psi)` | numeric vector | `required` | Honestly constructed numeric pseudo-outcomes. | No additional constraint stated in this parameter's Rd text. | An object of class `roscar_cate` with fit and fixed basis specification. |
| `fit_cate(basis)` | one-sided formula with intercept | `~.` | One-sided effect formula with intercept, e.g. `~ X1 + X2`. Spline knots are fixed by the original fit for prediction and bootstrap. | One-sided effect formula with intercept, e.g. | An object of class `roscar_cate` with fit and fixed basis specification. |
| `fit_cate(offset)` | numeric vector or NULL | `NULL` | Optional preliminary contrast on training rows. Supply its corresponding new-row values to the predict method. | No additional constraint stated in this parameter's Rd text. | An object of class `roscar_cate` with fit and fixed basis specification. |
| `fit_cate(learner)` | learner list with fit/predict functions | `learner_ols()` | Final squared-loss learner, unpenalized least squares by default. | No additional constraint stated in this parameter's Rd text. | An object of class `roscar_cate` with fit and fixed basis specification. |
| `bootstrap_cate(fit)` | roscar_fit object | `required` | Fitted `roscar_fit`. | No additional constraint stated in this parameter's Rd text. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `bootstrap_cate(B)` | integer count | `500` | Number of attempted case bootstrap resamples, at least two. | Number of attempted case bootstrap resamples, at least two. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `bootstrap_cate(grid)` | numeric matrix or data frame or NULL | `NULL` | Fixed covariate matrix/data frame for prediction; defaults to the original trial rows (standardization to their empirical distribution). | No additional constraint stated in this parameter's Rd text. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `bootstrap_cate(seed)` | integer scalar or NULL | `NULL` | Bootstrap seed. | No additional constraint stated in this parameter's Rd text. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `bootstrap_cate(subgroups)` | named list of logical or integer row selectors or NULL | `NULL` | Named logical/index vectors selecting rows of grid. | No additional constraint stated in this parameter's Rd text. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `bootstrap_cate(alpha)` | numeric scalar | `0.05` | Two-sided interval error probability, default 0.05. | No additional constraint stated in this parameter's Rd text. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `bootstrap_cate(groups)` | vector of IDs or labels (e.g., character, numeric, factor) or NULL | `NULL` | Optional labels on original trial rows for diagnostic summaries. | No additional constraint stated in this parameter's Rd text. | List with intervals, paired draws, subgroup intervals/draws, diagnostic draws, resampling/fold audit, failures, attempted and successful counts. |
| `diagnose(fit)` | roscar_fit object | `required` | A fitted `roscar_fit` from [borrow_cate()]. | No additional constraint stated in this parameter's Rd text. | A `roscar_diagnostics` list: source support summaries, prognostic score densities, aggregate calibration bins, row-level residuals and losses, four-baseline pseudo-outcome variances, arm losses, borrowing table, width ratios, and negative-transfer flag. Row-level elements must not be exported from protected-data analyses. Bounds are diagnostic prediction evidence, not a test of CATE validity or a proven variance reduction. |
| `diagnose(B)` | integer count | `500` in paper analyses; API `0` | Full trial-refitting bootstrap resamples for diagnostic bounds. Default zero uses an existing stored bootstrap or returns uncomputed bounds. | No additional constraint stated in this parameter's Rd text. | A `roscar_diagnostics` list: source support summaries, prognostic score densities, aggregate calibration bins, row-level residuals and losses, four-baseline pseudo-outcome variances, arm losses, borrowing table, width ratios, and negative-transfer flag. Row-level elements must not be exported from protected-data analyses. Bounds are diagnostic prediction evidence, not a test of CATE validity or a proven variance reduction. |
| `diagnose(seed)` | integer scalar or NULL | `fit$seed` | Seed for diagnostic bootstrap. | No additional constraint stated in this parameter's Rd text. | A `roscar_diagnostics` list: source support summaries, prognostic score densities, aggregate calibration bins, row-level residuals and losses, four-baseline pseudo-outcome variances, arm losses, borrowing table, width ratios, and negative-transfer flag. Row-level elements must not be exported from protected-data analyses. Bounds are diagnostic prediction evidence, not a test of CATE validity or a proven variance reduction. |
| `diagnose(groups)` | vector of IDs or labels (e.g., character, numeric, factor) or NULL | `NULL` | Optional site/subgroup labels on trial rows. Pooled results are always returned; subgroup bounds use the same resampled full pipeline. | No additional constraint stated in this parameter's Rd text. | A `roscar_diagnostics` list: source support summaries, prognostic score densities, aggregate calibration bins, row-level residuals and losses, four-baseline pseudo-outcome variances, arm losses, borrowing table, width ratios, and negative-transfer flag. Row-level elements must not be exported from protected-data analyses. Bounds are diagnostic prediction evidence, not a test of CATE validity or a proven variance reduction. |
| `diagnose(bins)` | integer count | `5` | Number of quantile bins for calibration, default five. | No additional constraint stated in this parameter's Rd text. | A `roscar_diagnostics` list: source support summaries, prognostic score densities, aggregate calibration bins, row-level residuals and losses, four-baseline pseudo-outcome variances, arm losses, borrowing table, width ratios, and negative-transfer flag. Row-level elements must not be exported from protected-data analyses. Bounds are diagnostic prediction evidence, not a test of CATE validity or a proven variance reduction. |
| `compare_methods(trial)` | roscar_trial list | `required` | Trial from [trial_data()], with observed-arm probabilities, IDs, strata, and optional positive analysis weights. | Trial from [trial_data()], with observed-arm probabilities, IDs, strata, and optional positive analysis weights. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(ext)` | roscar_external list or NULL | `NULL` | External source from [external_data()]. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(methods)` | character vector | `c("none", "racer", "A")` | Names from `none`, `racer`, `uncalibrated`, `oracle`, `interaction_ols`, `rlearner`, `causal_forest`, `procova_interaction`, `pooled`, `A`, `B`, `C`, `D`, `shared_only`. | Names from `none`, `racer`, `uncalibrated`, `oracle`, `interaction_ols`, `rlearner`, `causal_forest`, `procova_interaction`, `pooled`, `A`, `B`, `C`, `D`, `shared_only`. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(grid)` | numeric matrix or data frame | `trial$X` | Matrix/data frame of target covariates; defaults to trial rows. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(basis)` | one-sided formula with intercept | `~.` | Prespecified one-sided effect formula, with intercept. | Prespecified one-sided effect formula, with intercept. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(learners)` | named list of learners | `list()` | Named list of external, calibration, trial, final, and imputer learners. `trial_plus` and `trial_minus` optionally override individual arms. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(K)` | integer count | `5` | Number of grouped, arm-stratified folds; default 5. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(B)` | integer count | `500` in paper analyses; API `0` | Number of full trial bootstrap resamples; default 0; paper examples 500. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(seed)` | integer scalar or NULL | `NULL` | Integer seed for folds and default learners. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(subgroups)` | named list of logical or integer row selectors or NULL | `NULL` | Named logical/index vectors on `grid` for average effects. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(oracle_baseline)` | function or NULL | `NULL` | Known baseline function of trial covariates, simulations only. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
| `compare_methods(...)` | additional named arguments | `not supplied` | Additional transport settings. Unknown settings produce an error. | No additional constraint stated in this parameter's Rd text. | Named list; each method has `fit`, `prediction`, `interval`, `subgroups`, `status`, `error`, and `interval_method`. |
