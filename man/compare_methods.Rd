% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparators.R
\name{compare_methods}
\alias{compare_methods}
\title{Compare trial and external-prediction effect estimators}
\usage{
compare_methods(
  trial,
  ext = NULL,
  methods = c("none", "racer", "A"),
  grid = trial$X,
  basis = ~.,
  learners = list(),
  K = 5,
  B = 0,
  seed = NULL,
  subgroups = NULL,
  oracle_baseline = NULL,
  ...
)
}
\arguments{
\item{trial}{Trial from [trial_data()], with observed-arm probabilities, IDs,
strata, and optional positive analysis weights.}

\item{ext}{External source from [external_data()].}

\item{methods}{Names from `none`, `racer`, `uncalibrated`, `oracle`,
`interaction_ols`, `rlearner`, `causal_forest`, `procova_interaction`,
`pooled`, `A`, `B`, `C`, `D`, `shared_only`.}

\item{grid}{Matrix/data frame of target covariates; defaults to trial rows.}

\item{basis}{Prespecified one-sided effect formula, with intercept.}

\item{learners}{Named list of external, calibration, trial, final, and imputer
learners. `trial_plus` and `trial_minus` optionally override individual arms.}

\item{K}{Number of grouped, arm-stratified folds; default 5.}

\item{B}{Number of full trial bootstrap resamples; default 0; paper examples 500.}

\item{seed}{Integer seed for folds and default learners.}

\item{subgroups}{Named logical/index vectors on `grid` for average effects.}

\item{oracle_baseline}{Known baseline function of trial covariates, simulations only.}

\item{...}{Additional transport settings. Unknown settings produce an error.}
}
\value{
Named list; each method has `fit`, `prediction`, `interval`,
  `subgroups`, `status`, `error`, and `interval_method`.
}
\description{
All methods predict the same supplied grid and use the same prespecified
effect basis where applicable. The R-learner fits a cross-fitted trial outcome
nuisance and regresses outcome residuals on randomized treatment residuals.
Causal forest uses grf's own pointwise variance estimates. Other methods use
the conditional case bootstrap when B is positive; B = 0 computes no interval.
Failures and unsupported configurations are returned explicitly.
}
