% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{simulate_scenario}
\alias{simulate_scenario}
\title{Simulate a Prespecified Tutorial Scenario}
\usage{
simulate_scenario(
  name,
  n_r = NULL,
  n_o = 2000,
  seed = 20260914,
  test_seed = 20260901,
  variant = "nonprognostic",
  rho = 0,
  gamma = 1,
  kappa = 0,
  support_fraction = 0.1,
  delta_fraction = 0.02
)
}
\arguments{
\item{name}{Integer 1--6 or a string such as "scenario1"; "teaching"
selects the four-covariate example from Section 4.3.}

\item{n_r}{Trial sample size; defaults to 250 for Scenario 6 and 200 otherwise.}

\item{n_o}{External sample size.}

\item{seed}{Seed for the independent trial and external observations.}

\item{test_seed}{Separate seed for the fixed evaluation set.}

\item{variant}{Scenario 3 external outcome: "nonprognostic" or "reversed".}

\item{rho}{Scenario 4 recoverability parameter, either 0 or 0.8.}

\item{gamma}{Scenario 5 external control prognosis coefficient, 0 or 1.}

\item{kappa}{Scenario 5 treated-arm sine coefficient, 0 or 1.}

\item{support_fraction}{Scenario 6 nonzero prognostic-coefficient fraction.}

\item{delta_fraction}{Scenario 6 nonzero source-shift coefficient fraction.}
}
\value{
List containing validated trial and external data, analytic CATE
  vectors, the fixed test set, three profile rows, exact average and subgroup
  truths, the effect basis, the oracle baseline function, and generation metadata.
}
\description{
Scenarios 1--5 implement Section 8 of the tutorial. Scenario 6 is the
sparse extension requested in audit A6: 100 Gaussian covariates, ten
prognostic coefficients, two effect modifiers, and a two-coordinate
external-to-trial prognostic shift. Its exact coefficients are recorded
in the returned metadata and in paper/simulations/README.md.
}
\details{
The trial always has known balanced Bernoulli randomization. Independent
errors for both potential outcomes are generated before the observed
outcome is selected. The 2,000-row evaluation sample uses a separate seed
that does not depend on the replication seed. Thus repeated calls with
different replication seeds share the same evaluation covariates.
}
\examples{
sim <- simulate_scenario(1, n_r = 80, n_o = 200, seed = 42)
head(sim$truth_trial)
sim$truth_profiles
}
