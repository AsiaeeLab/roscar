% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learners.R
\name{learners}
\alias{learners}
\alias{learner_lasso}
\alias{learner_ridge}
\alias{learner_ols}
\alias{learner_intercept}
\alias{learner_gam}
\alias{learner_rf}
\alias{learner_sl}
\title{Regression learners for external prediction and trial calibration}
\usage{
learner_lasso(
  lambda = 10^seq(-4, 2, length.out = 25),
  nfolds = 5,
  seed = NULL,
  family = c("gaussian", "binomial"),
  intercept = TRUE,
  ...
)

learner_ridge(
  lambda = 10^seq(-4, 2, length.out = 25),
  nfolds = 5,
  seed = NULL,
  family = c("gaussian", "binomial"),
  intercept = TRUE,
  ...
)

learner_ols(family = c("gaussian", "binomial"), intercept = TRUE)

learner_intercept(family = c("gaussian", "binomial"))

learner_gam(k = 5, family = c("gaussian", "binomial"), ...)

learner_rf(num.trees = 500, seed = NULL, ...)

learner_sl(SL.library = c("SL.mean", "SL.glm"), nfolds = 5, seed = NULL, ...)
}
\arguments{
\item{lambda}{Positive penalty grid, defaulting to 25 log-spaced values from
1e-4 to 1e2. A single value disables tuning.}

\item{nfolds}{Number of tuning folds, default five.}

\item{seed}{Optional tuning seed, with no change to the caller's RNG state.}

\item{family}{Either `"gaussian"` or `"binomial"`.}

\item{intercept}{Whether to include an unpenalized intercept.}

\item{...}{Additional options to the backend. Core response, offset, fold,
and prediction-scale arguments are controlled by the interface.}

\item{k}{Basis size for each GAM smooth (automatically limited by the
number of distinct observed covariate values).}

\item{num.trees}{Number of ranger regression trees.}

\item{SL.library}{SuperLearner library; its constituent packages must be
installed by the user. Built-in wrappers resolve in the SuperLearner
namespace; custom wrappers can supply their lookup environment through
`env` in `...` (including the package's screening function `All`).}
}
\value{
A learner list with `fit`, `predict`, `name`, `family`, and settings.
}
\description{
A learner is a list containing `fit(X, y, weights, offset, ...)` and
`predict(fit, X, ...)`. Gaussian learners fit y minus the supplied offset;
predictions contain only that fitted correction. The caller adds the offset.
Binomial learners return a linear-predictor correction, so the caller uses
`plogis(offset + prediction)`. Prediction never reuses training offsets.
}
\details{
Penalized learners standardize predictors, retain an unpenalized intercept,
and select lambda by training-sample cross-validation. Supply `id` to `fit()`
to keep all copies of an independent unit together during tuning. The grid,
selected penalty, and tuning folds are retained in the fitted object.
Link-scale binary calibration is implemented but is not evaluated in the
paper's simulations. Random forests and SuperLearner provide Gaussian
residual regressions only; use a binomial GLM/GAM learner for link calibration.
}
