% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_api.R
\name{trial_data}
\alias{trial_data}
\title{Construct validated randomized trial data}
\usage{
trial_data(
  X,
  A,
  Y,
  pi = NULL,
  id = NULL,
  strata = NULL,
  shared = NULL,
  trial_only = NULL,
  external_only = NULL,
  weights = NULL
)
}
\arguments{
\item{X}{Finite numeric covariate matrix or numeric data frame.}

\item{A}{Treatment coded 0/1 or -1/+1.}

\item{Y}{Finite numeric outcome vector.}

\item{pi}{Probability of each participant's observed arm, strictly between
zero and one; a scalar is recycled. Defaults to observed arm proportions
within strata. This is not always the probability of treatment +1.}

\item{id}{Independent resampling unit identifier, defaulting to row number.}

\item{strata}{Randomization/design strata, defaulting to one stratum.}

\item{shared, trial_only, external_only}{Optional disjoint covariate block names
or column indices. Missing block assignments are inferred during transport.}

\item{weights}{Optional positive finite analysis weights (for example,
inverse probabilities of response). These do not replace randomization pi.}
}
\value{
A validated list of class `roscar_trial`, including `p_plus`, the
  probability of assignment to +1, and the covariate `blocks`.
}
\description{
The returned list uses the same numeric X, Y, A layout as [build_data()].
Categorical predictors must first be encoded into numeric columns. Supply
known design probabilities when available: an empirical arm proportion is
only the fallback. This constructor does not infer cluster randomization.
}
