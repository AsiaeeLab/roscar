% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{borrow_cate}
\alias{borrow_cate}
\alias{predict.roscar_fit}
\alias{print.roscar_fit}
\title{Borrow calibrated outcome predictions for a trial CATE}
\usage{
borrow_cate(
  trial,
  ext,
  recipe = c("A", "B", "C", "D"),
  learners = list(),
  K = 5,
  B = 0,
  grid = NULL,
  seed = NULL,
  basis = ~.,
  arm_ext = -1,
  method = NULL,
  shared = NULL,
  trial_only = NULL,
  external_only = NULL,
  embed_dim = NULL,
  pls_by_arm = FALSE,
  scale = c("mean", "link"),
  offset = NULL,
  folds = NULL,
  subgroups = NULL,
  ...
)

\method{predict}{roscar_fit}(object, newdata = object$trial$X, ...)

\method{print}{roscar_fit}(x, ...)
}
\arguments{
\item{trial}{Trial from [trial_data()], with observed-arm probabilities, IDs,
strata, and optional positive analysis weights.}

\item{ext}{External source from [external_data()].}

\item{recipe}{A (matched, two arms), B (one borrowed arm), C (MR-OSCAR
imputation), or D (linear CALM with externally supervised PLS).}

\item{learners}{Named list of external, calibration, trial, final, and imputer
learners. `trial_plus` and `trial_minus` optionally override individual arms.}

\item{K}{Number of grouped, arm-stratified folds; default 5.}

\item{B}{Number of full trial bootstrap resamples; default 0; paper examples 500.}

\item{grid}{Matrix/data frame of target covariates; defaults to trial rows.}

\item{seed}{Integer seed for folds and default learners.}

\item{basis}{Prespecified one-sided effect formula, with intercept.}

\item{arm_ext}{Borrowed arm for Recipe B, default -1; 0 also means control.}

\item{method}{Transport comparator: `"shared_only"` implements SR-OSCAR;
otherwise transport follows the selected recipe.}

\item{shared, trial_only, external_only}{Optional column-name covariate blocks.}

\item{embed_dim}{PLS dimension; NULL selects 1--3 by external validation.}

\item{pls_by_arm}{Fit arm-specific PLS directions instead of the pooled default.}

\item{scale}{Mean-scale or logistic link-scale calibration. Link calibration
needs binary outcome learners and was not evaluated in the paper simulations.}

\item{offset}{Use the cross-fitted preliminary-contrast correction. Defaults
to TRUE for Recipe C and FALSE otherwise.}

\item{folds}{Optional fixed grouped trial fold labels, reused across methods.}

\item{subgroups}{Named logical/index vectors on `grid` for average effects.}

\item{...}{Additional transport settings. Unknown settings produce an error.}

\item{object}{A fitted `roscar_fit`.}

\item{newdata}{New covariate matrix or data frame.}

\item{x}{A fitted object to print.}
}
\value{
A `roscar_fit` with folds, heldout intermediate predictions, final
  model, paired `trial_only` fit, transport metadata, and optional bootstrap.
}
\description{
Nuisance fits and tuning exclude each evaluation unit. The final regression
uses all held-out pseudo-outcomes; its training predictions are not an outer
evaluation of CATE accuracy. Recipe C uses a preliminary-contrast offset by
default; direct effect regression is the default for the other recipes.
}
